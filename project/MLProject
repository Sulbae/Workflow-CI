name: workflow-ci

conda_env: conda.yaml
env_manager: local

entry_points:
  train_tuning:
    parameters:
      experiment_name: {type: str}
    command: "python modelling_tuning.py --experiment_name {{experiment_name}}"